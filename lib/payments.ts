type PaymentOrder = { orderCode:string; total:number; name:string; phone:string; email?:string; address:string; paymentMethod:"bkash"|"card" };
function config() { const storeId=process.env.SSLCOMMERZ_STORE_ID; const password=process.env.SSLCOMMERZ_STORE_PASSWORD; if(!storeId||!password) throw new Error("Online payment is temporarily unavailable. Please select cash on delivery."); const sandbox=process.env.SSLCOMMERZ_SANDBOX!=="false"; return {storeId,password,base:sandbox?"https://sandbox.sslcommerz.com":"https://securepay.sslcommerz.com"}; }
export async function initiatePayment(order:PaymentOrder){ const {storeId,password,base}=config(); const site=(process.env.PUBLIC_SITE_URL||"http://127.0.0.1:3000").replace(/\/$/,""); const body=new URLSearchParams({store_id:storeId,store_passwd:password,total_amount:String(order.total),currency:"BDT",tran_id:order.orderCode,success_url:`${site}/api/payments/callback`,fail_url:`${site}/api/payments/fail`,cancel_url:`${site}/api/payments/cancel`,ipn_url:`${site}/api/payments/ipn`,cus_name:order.name,cus_email:order.email||"customer@fashionista.bd",cus_add1:order.address,cus_city:"Bangladesh",cus_country:"Bangladesh",cus_phone:order.phone,shipping_method:"YES",ship_name:order.name,ship_add1:order.address,ship_city:"Bangladesh",ship_country:"Bangladesh",product_name:`FashionistA order ${order.orderCode}`,product_category:"Clothing",product_profile:"general",multi_card_name:order.paymentMethod==="bkash"?"bkash":"visacard,mastercard,amexcard"}); const response=await fetch(`${base}/gwprocess/v4/api.php`,{method:"POST",headers:{"content-type":"application/x-www-form-urlencoded"},body}); const data=await response.json() as {status?:string;GatewayPageURL?:string;failedreason?:string}; if(data.status!=="SUCCESS"||!data.GatewayPageURL) throw new Error(data.failedreason||"The payment gateway could not start."); return data.GatewayPageURL; }
export async function validatePayment(valId:string){ const {storeId,password,base}=config(); const url=new URL(`${base}/validator/api/validationserverAPI.php`); url.search=new URLSearchParams({val_id:valId,store_id:storeId,store_passwd:password,format:"json"}).toString(); const response=await fetch(url); return response.json() as Promise<{status?:string;tran_id?:string;amount?:string;currency?:string;risk_level?:string}>; }
